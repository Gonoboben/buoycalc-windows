# Хронология проекта

Дата создания файла: 2026-06-30

Файл предназначен для последовательной фиксации изменений. Новые записи добавляются сверху или по версии, но без удаления старых записей.

## Формат записи

```text
Дата:
Версия / ветка / PR:
Что изменено:
Зачем изменено:
Затронутая архитектура:
Проверка:
Что специально не изменялось:
Следующий шаг:
```

---

## 2026-06-30 — фиксация правил ведения проекта

Версия / ветка / PR:

```text
project-governance-2026-06-30
```

Что изменено:

1. Добавлен управляющий документ `Docs/PROJECT_CONTROL.md`.
2. Добавлена архитектурная база `Docs/ARCHITECTURE_BASELINE.md`.
3. Добавлена хронология `Docs/CHRONOLOGY.md`.
4. Добавлен каталог для архитектурных решений `Docs/adr/`.

Зачем изменено:

Проект переводится в режим инженерного ведения через GitHub: с фиксацией архитектуры, хронологии, ограничений и проверок.

Затронутая архитектура:

Код и расчётная физика не затронуты. Изменены только документы управления проектом.

Проверка:

1. Код не менялся.
2. Solver не менялся.
3. PDF не менялся.
4. Интерфейс не менялся.
5. Документы согласованы с текущей зафиксированной линией BuoyCalc Windows и PDU.

Что специально не изменялось:

1. Физика расчёта.
2. Старый solver.
3. Новый solver.
4. Gate.
5. Fallback-форма.
6. PDF-схема.
7. 2D-окно.
8. README версии `v0.46.4`.

Следующий шаг:

По текущему README следующий прикладной шаг — `v0.46.5`: перевести 2D-окно на пользовательскую терминологию без изменения физики solver.

---

## Текущая контрольная точка BuoyCalc Windows

Дата фиксации: 2026-06-30

Текущая версия README:

```text
v0.46.4 — пользовательские статусы PDF
```

Состояние:

1. Физика расчёта не менялась.
2. Solver не менялся.
3. Gate не удалён.
4. Fallback-форма не удалена.
5. PDF-схема берёт координаты из расчётного слоя дискретных нагрузок.
6. Release build workflow остаётся прежним.

Ближайшая задача:

```text
v0.46.5 — 2D-окно: пользовательская терминология.
```
