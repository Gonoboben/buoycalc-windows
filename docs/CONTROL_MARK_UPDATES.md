# BuoyCalc — журнал обновлений контрольной отметки

Этот файл является append-only журналом к `docs/CONTROL_MARK.md`.
Он нужен, чтобы фиксировать продвижение без удаления и сокращения базовой контрольной отметки.

## v0.46.5 — 2D выбранная форма

Дата: 2026-06-29

Сверка с контрольной отметкой:

```text
Следующий шаг по контрольной отметке: v0.46.5 — 2D-схема: selected shape и пользовательские статусы.
```

Что сделано:

```text
Views/Mooring2DCanvas.cs
Services/AppInfo.cs
```

Смысл изменения:

```text
2D-canvas при доступной форме с дискретными элементами показывает одну выбранную форму.
Пользовательское сравнение старых форм в 2D убрано из отображения.
Необъяснённый технический WARNING убран из 2D-canvas.
```

Что не менялось:

```text
физика расчёта
solver
gate
внутренние диагностические слои
PDF builder
release workflow
```

Что не удалось сделать в этом коммите:

```text
Mooring2DWindow.axaml и его code-behind были заблокированы инструментом при попытке заменить старый бейдж версии.
Поэтому старый бейдж окна 2D может ещё оставаться в XAML.
Это не изменение физики, но это надо закрыть отдельным шагом.
```

Следующий шаг:

```text
v0.46.6 — аудит пользовательских строк и жёстко прописанных версий.
Перед 3D нужно убрать старые версии из XAML и добавить CI-проверку пользовательских текстов.
```
