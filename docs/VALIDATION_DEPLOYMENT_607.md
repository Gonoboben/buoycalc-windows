# Deployment validation package #607

Production core: `0db1e7cc17e309fc1402fe58b265d2e95c2d4d39`.
No production source or frozen baseline changes.

## Matrix and provenance
The 210 requested depth/soil/mass slots are explicitly enumerated.
54 slots can run: six depths, three educational soil presets, and concrete 500/1000 kg catalog plus the existing Series A 3000 kg / 1.2 m3 fixture.
156 slots are BLOCKED: missing soil model and/or explicit anchor volume. The mud preset does not establish clay behavior.
Additional sweeps use existing hardware fixtures and explicit synthetic environmental inputs.
Wave periods are 5 and 8 seconds. Currents and waves are not regional climate data.
The 380 m BL-820 field case is BLOCKED for missing vessel loads, equipment and site evidence.

## Criteria
Execution, invariant/authority tests, limited independent checks, full numerical validation, field suitability and presentation are distinct columns.
Limited independent checks cover geometry necessary conditions and zero-load identities only.
No global claim of full X/Z/tension numerical agreement is made.
Selected endpoint chord is checked with 1e-6 m tolerance; residual is recorded, including non-Accepted fallback.
Production segmentation bound uses 1e-12 m rounding allowance. Segment length sum uses 1e-6 m; signed water weight sum uses 1e-7 kg. None is an acceptance epsilon.
RejectedPhysical must retain exact diagnostic code/text, terminal verdict and absent selected geometry/F1-F4.
User report text is checked for rejection. UI/2D/PDF rendering remains PENDING.

## Execution and evidence
`validation-deployment-campaign.yml` runs four independent packages on the exact pushed validation branch:
new deployment matrix, unchanged Series A, feedback diagnostics, physical rejection controls.
Production file identity is checked against the pinned core SHA.
Scenario JSON contains full inputs, sources, calculation outputs, selected nodes/authority, diagnostics and criteria/deviations.
JSON/CSV/Markdown rollups are uploaded even when tests fail.
The new campaign exits 2 on failing executed criteria, while BLOCKED/PENDING remain explicit.
Series A still returns zero with findings, including its obsolete selected-F4-on-rejection criterion. Do not treat that as an unfixed production defect.

## Delivery limitation
The requested PDF is not created: the available PDF router requires a runtime that cannot be opened in this session. A readable Markdown evidence report is generated by the campaign; it is not a PDF substitute completion claim.

## Before merge
Inspect actual artifacts and exact-head required CI. No merge until user explicitly authorizes the specific SHA.
No release, RC branch, tag, physics, architecture, test baseline or 3D changes.
