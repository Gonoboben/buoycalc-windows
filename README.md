# BuoyCalc Windows v0.3

Windows-ветка проекта BuoyCalc на **C# + Avalonia**.

Это не автоматическая конвертация SwiftUI-проекта, а портирование расчётной логики и интерфейса в desktop-приложение:

```text
SwiftUI iPhone prototype
↓
C# / Avalonia Windows desktop prototype
```

## Статус

v0.1 успешно запустилась на Windows в Visual Studio.

v0.2 улучшила общий вид главного окна.

v0.3 улучшает редактор последовательности постановки. Расчётное ядро не менялось.

## Что уже перенесено

1. условия среды;
2. буй;
3. последовательность постановки;
4. линии;
5. соединители;
6. приборы / нагрузки;
7. якорь;
8. грунт;
9. расчёт плавучести;
10. расчёт сопротивления течению;
11. упрощённая волновая нагрузка;
12. натяжение;
13. удержание якоря;
14. проверки системы;
15. текстовый отчёт.

## Что изменилось в v0.3

1. поле `Kind` заменено на выпадающий список `Line / Connector / Payload`;
2. `RopePresetId` заменён на выпадающий список пресетов линии;
3. `ConnectorPresetId` заменён на выпадающий список пресетов соединителей;
4. добавлено удаление элемента последовательности;
5. добавлено перемещение элемента вверх / вниз;
6. добавлено дублирование элемента;
7. добавлена сводка последовательности:
   - количество активных элементов;
   - суммарная длина линий;
   - количество соединителей;
   - масса приборов.

## Что пока не перенесено

1. сохранение проектов;
2. пользовательская библиотека элементов;
3. PDF-экспорт;
4. 2D-визуализация;
5. визуальная цепочка постановки;
6. источники данных;
7. импорт / экспорт;
8. редактирование библиотеки.

Эти блоки лучше переносить после того, как базовый Windows-интерфейс станет удобным.

## Структура

```text
buoycalc-windows
├─ BuoyCalc.Windows.csproj
├─ Program.cs
├─ App.axaml
├─ App.axaml.cs
├─ Models
│  ├─ EngineeringModels.cs
│  └─ Catalogs.cs
├─ Services
│  └─ ReportBuilder.cs
├─ ViewModels
│  ├─ ViewModelBase.cs
│  ├─ RelayCommand.cs
│  ├─ AssemblyItemViewModel.cs
│  └─ MainWindowViewModel.cs
├─ Views
│  ├─ MainWindow.axaml
│  └─ MainWindow.axaml.cs
└─ Docs
   └─ MIGRATION_PLAN.md
```

## Как обновиться локально

Если репозиторий уже клонирован, в Visual Studio можно использовать:

```text
Git → Pull
```

Или через терминал, если установлен Git:

```bash
git pull
```

Затем запустить проект заново.

## Как запустить на Windows

### 1. Установить .NET SDK

Установить .NET SDK 8 или новее.

### 2. Открыть папку проекта

Открыть папку репозитория `buoycalc-windows` в Visual Studio, Rider или VS Code.

### 3. Восстановить пакеты

В терминале внутри папки проекта:

```bash
dotnet restore
```

### 4. Запустить

```bash
dotnet run
```

## Технологии

- C#
- .NET 8
- Avalonia 12.0.5
- XAML
- MVVM-подход без внешних MVVM-библиотек

## Следующий шаг

Рекомендуемый v0.4:

1. добавить сохранение проекта в JSON;
2. добавить открытие проекта из JSON;
3. добавить кнопку `Новый проект`;
4. добавить кнопку `Сохранить проект`;
5. добавить кнопку `Открыть проект`;
6. затем перейти к пользовательской библиотеке элементов.
