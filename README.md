# BuoyCalc Windows v0.21.1

Windows-ветка проекта BuoyCalc на **C# + Avalonia**.

Это портирование расчётной логики и интерфейса в desktop-приложение:

```text
SwiftUI iPhone prototype
↓
C# / Avalonia Windows desktop prototype
```

## Статус

v0.21.1 исправляет обрезку полного отчёта в PDF и добавляет отдельное окно для просмотра полного текстового Markdown-отчёта.

## Что изменилось в v0.21.1

1. в `PdfReportBuilder` убрано ограничение `maxLines: 180`;
2. строка `... отчёт обрезан для компактного PDF` больше не должна появляться;
3. полный текстовый отчёт теперь продолжается на следующих страницах PDF;
4. добавлено окно `ReportTextWindow`;
5. в верхнюю панель главного окна добавлена кнопка `Отчёт текстом...`;
6. окно отчёта показывает полный `ReportText` без обрезки;
7. текст можно выделить и скопировать.

## Что осталось от v0.21

v0.21 добавляет первый расчёт натяжений по сегментам линии. После v0.20 у нас уже были сегменты, глубины и локальные силы течения. Теперь для каждого сегмента оцениваются вес в воде, накопленная горизонтальная сила, накопленная вертикальная сила, натяжение и угол от вертикали.

## Что важно

Это ещё не полный `moordyn`. Сейчас натяжение считается по накопленным силам снизу вверх, но координаты линии пока не итерационные. То есть это переходная физическая модель:

```text
сегменты линии
↓
локальные силы
↓
накопленные силы снизу вверх
↓
оценочное натяжение
↓
следующий этап: координаты узлов и форма линии
```

## Как обновиться локально

```text
Git → Pull
```

Затем:

```text
Build → Clean Solution
Build → Rebuild Solution
```

## Как проверить

1. Запустить программу.
2. Выполнить расчёт.
3. Нажать `Отчёт текстом...` в верхней панели.
4. Проверить, что открылось отдельное окно с полным Markdown-отчётом.
5. Нажать `Экспорт PDF`.
6. Открыть PDF.
7. Проверить, что строка `... отчёт обрезан для компактного PDF` больше не появляется.
8. Проверить, что длинный отчёт продолжается на следующих страницах.

## Где хранятся пользовательские библиотеки

```text
Документы\BuoyCalc\Libraries\user-buoys.json
Документы\BuoyCalc\Libraries\user-ropes.json
Документы\BuoyCalc\Libraries\user-connectors.json
Документы\BuoyCalc\Libraries\user-anchors.json
Документы\BuoyCalc\Libraries\user-payloads.json
```

## Следующий шаг

Рекомендуемый v0.22:

1. перейти от оценочной глубины сегмента к узлам линии;
2. построить набор узлов `X/Z` по длине линии;
3. использовать рассчитанный угол сегмента для построения формы;
4. добавить таблицу узлов;
5. после этого заменить 2D-схему на отображение рассчитанной формы линии.
