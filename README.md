# BuoyCalc Windows v0.7

Windows-ветка проекта BuoyCalc на **C# + Avalonia**.

Это не автоматическая конвертация SwiftUI-проекта, а портирование расчётной логики и интерфейса в desktop-приложение:

```text
SwiftUI iPhone prototype
↓
C# / Avalonia Windows desktop prototype
```

## Статус

v0.1 успешно запустилась на Windows в Visual Studio.

v0.2 улучшила общий вид главного окна.

v0.3 улучшила редактор последовательности постановки.

v0.3.1 сделала карточки последовательности чище: поля показываются по типу элемента.

v0.4 добавила базовое сохранение и загрузку проекта в JSON.

v0.4.1 добавила UX-правки: подсказки к полям, системные диалоги выбора файла и соединитель без поля количества.

v0.5 добавила первый блок пользовательской библиотеки элементов — библиотеку буёв.

v0.5.1 упростила работу с библиотекой буёв: выбранный буй применяется автоматически, добавлено удаление пользовательского буя.

v0.6 добавила отдельное окно `Библиотека элементов`. Создание, редактирование и удаление буёв вынесены из главного окна.

v0.6.1 добавила подсказки `?` внутри библиотеки элементов и перенесла кнопку открытия библиотеки в верхнюю панель главного окна.

v0.6.2 исправила стиль верхней кнопки библиотеки.

v0.7 добавляет библиотеку линий / буйрепов. Пользовательские линии появляются в выпадающих списках последовательности. Расчётное ядро не менялось.

## Что уже перенесено

1. условия среды;
2. буй;
3. последовательность постановки;
4. линии;
5. соединители;
6. приборы / нагрузки;
7. якорь;
8. грунт;
9. расчёт плавучести;
10. расчёт сопротивления течению;
11. упрощённая волновая нагрузка;
12. натяжение;
13. удержание якоря;
14. проверки системы;
15. текстовый отчёт;
16. сохранение проекта в JSON;
17. загрузка проекта из JSON;
18. создание нового проекта;
19. подсказки к исходным данным;
20. встроенные пресеты буёв;
21. пользовательская библиотека буёв;
22. удаление пользовательских буёв;
23. отдельное окно библиотеки элементов;
24. подсказки внутри окна библиотеки;
25. встроенные пресеты линий;
26. пользовательская библиотека линий;
27. пользовательские линии в последовательности постановки.

## Что изменилось в v0.7

1. добавлена модель `RopeLibraryItem`;
2. добавлен сервис `RopeLibraryStorage`;
3. в окне `Библиотека элементов` появился раздел `Линии`;
4. можно создать новую пользовательскую линию;
5. можно сохранить линию;
6. можно удалить пользовательскую линию;
7. встроенные линии удалить нельзя;
8. пользовательские линии сохраняются в JSON;
9. выпадающий список `Пресет линии` в последовательности теперь берёт данные из библиотеки линий;
10. после закрытия окна библиотеки список линий в карточках последовательности обновляется.

Пользовательская библиотека буёв хранится по пути:

```text
Документы\BuoyCalc\Libraries\user-buoys.json
```

Пользовательская библиотека линий хранится по пути:

```text
Документы\BuoyCalc\Libraries\user-ropes.json
```

## Архитектурное решение

Главное окно постепенно становится окном проекта:

```text
Главное окно проекта
├─ выбрать буй из библиотеки
├─ собрать последовательность элементов
├─ задать условия
├─ выбрать / задать якорь
└─ рассчитать / сформировать отчёт
```

Отдельное окно библиотеки постепенно становится общим редактором элементов:

```text
Библиотека элементов
├─ буи
├─ линии
├─ соединители
├─ приборы
└─ якоря
```

## Что пока не перенесено

1. библиотека соединителей;
2. библиотека приборов;
3. библиотека якорей;
4. выбор якоря из библиотеки в главном окне;
5. PDF-экспорт;
6. 2D-визуализация;
7. визуальная цепочка постановки;
8. источники данных.

## Структура

```text
buoycalc-windows
├─ BuoyCalc.Windows.csproj
├─ Program.cs
├─ App.axaml
├─ App.axaml.cs
├─ Models
│  ├─ EngineeringModels.cs
│  ├─ Catalogs.cs
│  ├─ ProjectDtos.cs
│  ├─ BuoyLibraryModels.cs
│  └─ RopeLibraryModels.cs
├─ Services
│  ├─ ReportBuilder.cs
│  ├─ ProjectJsonStorage.cs
│  ├─ IProjectFileDialogService.cs
│  ├─ BuoyLibraryStorage.cs
│  └─ RopeLibraryStorage.cs
├─ ViewModels
│  ├─ ViewModelBase.cs
│  ├─ RelayCommand.cs
│  ├─ AssemblyItemViewModel.cs
│  ├─ MainWindowViewModel.cs
│  └─ ElementLibraryViewModel.cs
├─ Views
│  ├─ MainWindow.axaml
│  ├─ MainWindow.axaml.cs
│  ├─ ElementLibraryWindow.axaml
│  └─ ElementLibraryWindow.axaml.cs
└─ Docs
   └─ MIGRATION_PLAN.md
```

## Как обновиться локально

Если репозиторий уже клонирован, в Visual Studio можно использовать:

```text
Git → Pull
```

Затем запустить проект заново.

## Как проверить библиотеку линий

1. В верхней панели главного окна нажать `Библиотека элементов...`.
2. Открыть вкладку `Линии`.
3. Выбрать встроенную линию и посмотреть параметры.
4. Нажать `Новая линия`.
5. Заполнить название, материал, диаметр, MBL, вес в воде и Cd.
6. Нажать `Сохранить`.
7. Проверить, что новая линия появилась в списке.
8. Закрыть окно библиотеки.
9. В главном окне добавить элемент `+ линия`.
10. Открыть выпадающий список `Пресет линии` и выбрать пользовательскую линию.
11. Нажать `Рассчитать систему`.

## Как проверить сохранение проекта

1. Изменить название проекта.
2. Выбрать или создать буй.
3. Добавить пользовательскую линию в последовательность.
4. Нажать `Сохранить как...` и выбрать папку/имя файла.
5. Нажать `Новый`.
6. Нажать `Загрузить...` и выбрать сохранённый файл.
7. Проверить, что проект восстановился.

## Как запустить на Windows

### 1. Установить .NET SDK

Установить .NET SDK 8 или новее.

### 2. Открыть папку проекта

Открыть папку репозитория `buoycalc-windows` в Visual Studio, Rider или VS Code.

### 3. Восстановить пакеты

В терминале внутри папки проекта:

```bash
dotnet restore
```

### 4. Запустить

```bash
dotnet run
```

## Технологии

- C#
- .NET 8
- Avalonia 12.0.5
- XAML
- JSON
- MVVM-подход без внешних MVVM-библиотек

## Следующий шаг

Рекомендуемый v0.8:

1. добавить библиотеку соединителей;
2. добавить пользовательские соединители в карточку `Connector`;
3. затем добавить библиотеку якорей и выбор якоря в главном окне.
