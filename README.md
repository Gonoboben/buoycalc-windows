# BuoyCalc Windows v0.5.1

Windows-ветка проекта BuoyCalc на **C# + Avalonia**.

Это не автоматическая конвертация SwiftUI-проекта, а портирование расчётной логики и интерфейса в desktop-приложение:

```text
SwiftUI iPhone prototype
↓
C# / Avalonia Windows desktop prototype
```

## Статус

v0.1 успешно запустилась на Windows в Visual Studio.

v0.2 улучшила общий вид главного окна.

v0.3 улучшила редактор последовательности постановки.

v0.3.1 сделала карточки последовательности чище: поля показываются по типу элемента.

v0.4 добавила базовое сохранение и загрузку проекта в JSON.

v0.4.1 добавила UX-правки: подсказки к полям, системные диалоги выбора файла и соединитель без поля количества.

v0.5 добавила первый блок пользовательской библиотеки элементов — библиотеку буёв.

v0.5.1 упрощает работу с библиотекой буёв: выбранный буй применяется автоматически, добавлено удаление пользовательского буя. Расчётное ядро не менялось.

## Что уже перенесено

1. условия среды;
2. буй;
3. последовательность постановки;
4. линии;
5. соединители;
6. приборы / нагрузки;
7. якорь;
8. грунт;
9. расчёт плавучести;
10. расчёт сопротивления течению;
11. упрощённая волновая нагрузка;
12. натяжение;
13. удержание якоря;
14. проверки системы;
15. текстовый отчёт;
16. сохранение проекта в JSON;
17. загрузка проекта из JSON;
18. создание нового проекта;
19. подсказки к исходным данным;
20. встроенные пресеты буёв;
21. пользовательская библиотека буёв;
22. удаление пользовательских буёв.

## Что изменилось в v0.5.1

1. кнопка `Применить` удалена;
2. выбор буя в списке сразу заполняет поля буя;
3. добавлена кнопка `Удалить буй`;
4. удалить можно только пользовательский буй;
5. встроенные пресеты удалить нельзя;
6. сохранение пользовательского буя осталось в карточке как временный отладочный вариант.

Пользовательская библиотека буёв хранится по пути:

```text
Документы\BuoyCalc\Libraries\user-buoys.json
```

## Архитектурное решение на будущее

Текущая карточка `Буй` в главном окне — временный отладочный интерфейс.

Дальше правильнее разделить приложение так:

```text
Главное окно проекта
├─ выбрать буй из библиотеки
├─ выбрать последовательность элементов
├─ выбрать якорь из библиотеки
└─ рассчитать / сформировать отчёт

Отдельное окно библиотеки
├─ буи
├─ линии
├─ соединители
├─ приборы
└─ якоря
```

В главном окне должны остаться только выбор и расчёт. Создание, редактирование и удаление элементов лучше вынести в отдельное окно `Библиотека элементов`.

## Что пока не перенесено

1. отдельное окно библиотеки элементов;
2. пользовательская библиотека линий;
3. пользовательская библиотека соединителей;
4. пользовательская библиотека якорей;
5. PDF-экспорт;
6. 2D-визуализация;
7. визуальная цепочка постановки;
8. источники данных.

## Структура

```text
buoycalc-windows
├─ BuoyCalc.Windows.csproj
├─ Program.cs
├─ App.axaml
├─ App.axaml.cs
├─ Models
│  ├─ EngineeringModels.cs
│  ├─ Catalogs.cs
│  ├─ ProjectDtos.cs
│  └─ BuoyLibraryModels.cs
├─ Services
│  ├─ ReportBuilder.cs
│  ├─ ProjectJsonStorage.cs
│  ├─ IProjectFileDialogService.cs
│  └─ BuoyLibraryStorage.cs
├─ ViewModels
│  ├─ ViewModelBase.cs
│  ├─ RelayCommand.cs
│  ├─ AssemblyItemViewModel.cs
│  └─ MainWindowViewModel.cs
├─ Views
│  ├─ MainWindow.axaml
│  └─ MainWindow.axaml.cs
└─ Docs
   └─ MIGRATION_PLAN.md
```

## Как обновиться локально

Если репозиторий уже клонирован, в Visual Studio можно использовать:

```text
Git → Pull
```

Затем запустить проект заново.

## Как проверить библиотеку буёв

1. Открыть карточку `Буй`.
2. Выбрать встроенный пресет из выпадающего списка.
3. Проверить, что объём, масса, площадь и Cd изменились сразу после выбора.
4. Изменить название и параметры буя.
5. Нажать `Сохранить буй`.
6. Нажать `Обновить`.
7. Проверить, что пользовательский буй появился в списке.
8. Выбрать пользовательский буй и нажать `Удалить буй`.
9. Проверить, что встроенные буи не удаляются.
10. Нажать `Рассчитать систему` и проверить отчёт.

## Как проверить сохранение проекта

1. Изменить название проекта.
2. Выбрать или сохранить буй.
3. Добавить или удалить элемент последовательности.
4. Нажать `Сохранить как...` и выбрать папку/имя файла.
5. Нажать `Новый`.
6. Нажать `Загрузить...` и выбрать сохранённый файл.
7. Проверить, что проект восстановился.

## Как запустить на Windows

### 1. Установить .NET SDK

Установить .NET SDK 8 или новее.

### 2. Открыть папку проекта

Открыть папку репозитория `buoycalc-windows` в Visual Studio, Rider или VS Code.

### 3. Восстановить пакеты

В терминале внутри папки проекта:

```bash
dotnet restore
```

### 4. Запустить

```bash
dotnet run
```

## Технологии

- C#
- .NET 8
- Avalonia 12.0.5
- XAML
- JSON
- MVVM-подход без внешних MVVM-библиотек

## Следующий шаг

Рекомендуемый v0.6:

1. создать отдельное окно `Библиотека элементов`;
2. перенести туда создание, редактирование и удаление буёв;
3. оставить в главном окне только выбор буя;
4. затем добавить туда линии, соединители и якоря.
